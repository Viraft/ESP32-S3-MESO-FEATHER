# Hardware Directory

Place all KiCad source project files here.

Expected file types:
- `.kicad_pro`
- `.kicad_sch`
- `.kicad_pcb`
- project libraries/footprints if used

After adding files, open the project in KiCad and run ERC/DRC before generating production outputs.
